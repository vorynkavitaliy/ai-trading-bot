node_modules/
dist/
.env
*.log
.DS_Store

# Secrets
accounts.json
.claude/settings.local.json
.mcp.json

# Pure runtime — no value in git history
vault/state/cycle-counter.json     # resets per /clear
.claude/scheduled_tasks.lock        # harness state

# 2026-05-18: vault/ wiped; ignore re-creation by stale tools
vault/

# srcNew backtest data cache (large, re-downloadable)
srcNew/data/cache/
srcNew/live/state/

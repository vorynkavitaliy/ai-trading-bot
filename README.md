🔧 Полный запуск с нуля (если когда-то восстанавливать)

1. Инфраструктура (Docker)

cd /root/Projects/ai-trading-bot
npm run infra:up # Postgres 5433
npm run db:migrate # накатить все миграции

2. Данные (если БД пустая)

npm run data:backfill # OHLCV за 365д для 13 пар (~10-15 мин)
npm run cg:backfill # Coinglass для 13 пар (~3-5 мин)

3. Cron (фоновый цикл)

npm run trader:cron:install # _/5 _ \* \* \* cycle.sh
npm run trader:status # проверь что cron установлен

4. (опционально) tmux + Claude для ручного сопровождения

Cron уже исполняет hot-path. Claude нужен только для ad-hoc:
news-halt (PAUSE.md), бэктестов, отладки.

npm run trader:start # новая tmux сессия + claude --continue
# (если уже есть сессия — `npm run trader:attach`)

Detach: Ctrl+B → D

---

📊 Команды управления

┌────────────────────────────┬────────────────────────────────────┐
│ Что хочу │ Команда │
├────────────────────────────┼────────────────────────────────────┤
│ Что сейчас происходит? │ npm run trader:status │
├────────────────────────────┼────────────────────────────────────┤
│ Лог cron в реал-тайме │ npm run trader:logs │
├────────────────────────────┼────────────────────────────────────┤
│ Дневной P&L │ npm run trader:pnl │
├────────────────────────────┼────────────────────────────────────┤
│ Все сделки за 48ч │ npx tsx │
│ │ src/tools/diagnostics/ │
│ │ trades-status.ts │
├────────────────────────────┼────────────────────────────────────┤
│ Reconcile вручную │ npm run reconcile │
├────────────────────────────┼────────────────────────────────────┤
│ Сделать smoke-test cron │ npm run trader:cycle │
├────────────────────────────┼────────────────────────────────────┤
│ Подключиться к tmux │ npm run trader:attach │
├────────────────────────────┼────────────────────────────────────┤
│ Остановить tmux (cron │ npm run trader:stop │
│ остаётся) │ │
├────────────────────────────┼────────────────────────────────────┤
│ Удалить cron │ npm run trader:cron:uninstall │
└────────────────────────────┴────────────────────────────────────┘

---

🚨 Что делать если что-то сломалось

Бот шлёт мусорные сделки?
→ Создай vault/Watchlist/PAUSE.md (любой контент) — я в /loop увижу и
не буду открывать новые. Cron продолжит мониторинг.

Хочу выйти из всех позиций руками?
→ Через Bybit web-интерфейс, или скрипт-аварийка npx tsx -e "..."
(нужно отдельно собрать).

Полная остановка:
npm run trader:cron:uninstall # cron больше не запускает cycle
npm run trader:stop # tmux + claude убиты
